export default function Footer() {
  return (
    <footer className="border-t border-white/10 bg-slate-950">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-8 text-sm text-slate-400 sm:px-6 sm:flex-row sm:items-center sm:justify-between">
        <p>Serving Ruaka, Gachie, and Gathiga.</p>
        <p>Single-admin neighborhood delivery MVP with M-PESA checkout.</p>
      </div>
    </footer>
  );
}
